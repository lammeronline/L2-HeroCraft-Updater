<Window x:Class="PatchBuilder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Patch Builder" Height="960" Width="980" MinHeight="900" MinWidth="900"
        Background="#111318" Foreground="#F4F6F8" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B2028" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#A7B0BD" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2FD17C" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Foreground" Value="#F7FAFC" />
            <Setter Property="Background" Value="#313B49" />
            <Setter Property="BorderBrush" Value="#465364" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Foreground" Value="#F4F6F8" />
            <Setter Property="Background" Value="#12161D" />
            <Setter Property="BorderBrush" Value="#3E4958" />
            <Setter Property="CaretBrush" Value="#F4F6F8" />
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Background" Value="#11161D" />
            <Setter Property="BorderBrush" Value="#303947" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Patch Builder" FontSize="32" FontWeight="Bold" />
                <TextBlock Text="Manifest generator" Foreground="{StaticResource TextMutedBrush}" FontSize="14" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="ValidateButton" Content="VALIDATE" Width="132" Height="48"
                        Margin="0,0,10,0" Click="ValidateButton_Click" />
                <Button x:Name="GenerateButton" Content="GENERATE" Width="180" Height="48"
                        Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}"
                        Foreground="#07110B" FontSize="16" Click="GenerateButton_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8" Margin="0,0,0,18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Text="Patch folder" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="SourceDirectoryBox" Grid.Row="0" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="0" Grid.Column="2" Content="Browse" Width="96" Margin="0,0,0,10" Click="BrowseSourceButton_Click" />

                <TextBlock Grid.Row="1" Text="Source URL" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="BaseUrlBox" Grid.Row="1" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="1" Grid.Column="2" Content="Local" Width="96" Margin="0,0,0,10" Click="UseLocalButton_Click" />

                <TextBlock Grid.Row="2" Text="Channel" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="ChannelBox" Grid.Row="2" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="2" Grid.Column="2" Content="Apply" Width="96" Margin="0,0,0,10" Click="ApplyChannelButton_Click" />

                <TextBlock Grid.Row="3" Text="Manifest file" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="OutputManifestBox" Grid.Row="3" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="3" Grid.Column="2" Content="Save as" Width="96" Margin="0,0,0,10" Click="BrowseOutputButton_Click" />

                <TextBlock Grid.Row="4" Text="Version" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="VersionBox" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10" />

                <TextBlock Grid.Row="5" Text="Extra files" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <CheckBox x:Name="ReportExtraFilesBox" Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                          Content="Report files not listed in manifest" Foreground="#F4F6F8"
                          Margin="0,0,0,10" />

                <TextBlock Grid.Row="6" Text="Compression" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <CheckBox x:Name="CompressFilesBox" Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                          Content="Compress patch files as .gz" Foreground="#F4F6F8"
                          Margin="0,0,0,10" />

                <TextBlock Grid.Row="7" Text="Compressed out" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="CompressedOutputBox" Grid.Row="7" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="7" Grid.Column="2" Content="Browse" Width="96" Margin="0,0,0,10" Click="BrowseCompressedOutputButton_Click" />

                <TextBlock Grid.Row="8" Text="Ignore" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Top" Margin="0,6,0,0" />
                <TextBox x:Name="IgnoreRulesBox" Grid.Row="8" Grid.Column="1" Grid.ColumnSpan="2" Height="82"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="NoWrap"
                         FontFamily="Consolas" FontSize="12" Margin="0,0,0,10" />

                <TextBlock Grid.Row="9" Text="Launcher exe" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="LauncherFileBox" Grid.Row="9" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="9" Grid.Column="2" Content="Browse" Width="96" Margin="0,0,0,10" Click="BrowseLauncherButton_Click" />

                <TextBlock Grid.Row="10" Text="Launcher URL" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="LauncherUrlBox" Grid.Row="10" Grid.Column="1" Grid.ColumnSpan="2" />
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8" Margin="0,0,0,18">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="StatusText" Text="Idle" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock x:Name="FileCountText" Grid.Column="1" Text="0 files" Foreground="{StaticResource TextMutedBrush}" />
                    </Grid>
                    <ProgressBar x:Name="MainProgressBar" Minimum="0" Maximum="100" Value="0" />
                    <TextBlock x:Name="CurrentFileText" Text="Waiting" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Log" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />
                    <TextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="3" Text="Ready" Foreground="{StaticResource TextMutedBrush}" Margin="0,18,0,0" />
    </Grid>
</Window>

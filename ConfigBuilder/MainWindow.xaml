<Window x:Class="ConfigBuilder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Config Builder" Height="780" Width="900" MinHeight="720" MinWidth="820"
        Background="#111318" Foreground="#F4F6F8" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B2028" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#A7B0BD" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2FD17C" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Foreground" Value="#F7FAFC" />
            <Setter Property="Background" Value="#313B49" />
            <Setter Property="BorderBrush" Value="#465364" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Foreground" Value="#F4F6F8" />
            <Setter Property="Background" Value="#12161D" />
            <Setter Property="BorderBrush" Value="#3E4958" />
            <Setter Property="CaretBrush" Value="#F4F6F8" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D7DCE3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Config Builder" FontSize="32" FontWeight="Bold" />
                <TextBlock Text="Launcher behavior config" Foreground="{StaticResource TextMutedBrush}" FontSize="14" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="LOAD" Width="112" Height="48" Margin="0,0,10,0" Click="LoadButton_Click" />
                <Button Content="SAVE" Width="150" Height="48" Background="{StaticResource AccentBrush}"
                        BorderBrush="{StaticResource AccentBrush}" Foreground="#07110B" FontSize="16"
                        Click="SaveButton_Click" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Text="Config file" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="ConfigFileBox" Grid.Row="0" Grid.Column="1" Margin="0,0,10,10" />
                <Button Grid.Row="0" Grid.Column="2" Content="Browse" Width="96" Margin="0,0,0,10" Click="BrowseConfigButton_Click" />

                <TextBlock Grid.Row="1" Text="Manifest URL" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="ManifestUrlBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10" />

                <TextBlock Grid.Row="2" Text="Play button" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="PlayButtonTextBox" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10" />

                <TextBlock Grid.Row="3" Text="Default mode" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="DefaultDisplayModeBox" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10" />

                <TextBlock Grid.Row="4" Text="Default resolution" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <TextBox x:Name="DefaultResolutionBox" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10" />

                <TextBlock Grid.Row="5" Text="Options" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                <StackPanel Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,10">
                    <CheckBox x:Name="ShowClientSettingsBox" Content="Show client settings" Margin="0,0,22,0" />
                    <CheckBox x:Name="RequireUpdateBeforePlayBox" Content="Check before play" Margin="0,0,22,0" />
                    <CheckBox x:Name="DefaultAudioMuteBox" Content="Mute audio by default" />
                </StackPanel>

                <TextBlock Grid.Row="6" Text="Game exe list" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Top" Margin="0,6,0,0" />
                <TextBox x:Name="GameExecutablesBox" Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Height="76"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                         TextWrapping="NoWrap" Margin="0,0,0,10" />

                <TextBlock Grid.Row="7" Text="Resolutions" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Top" Margin="0,6,0,0" />
                <TextBox x:Name="ResolutionsBox" Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Height="104"
                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                         TextWrapping="NoWrap" Margin="0,0,0,10" />

                <TextBox x:Name="LogBox" Grid.Row="8" Grid.ColumnSpan="3" IsReadOnly="True" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Text="Upload config.json to /updater/config.json" Foreground="{StaticResource TextMutedBrush}" Margin="0,18,0,0" />
    </Grid>
</Window>

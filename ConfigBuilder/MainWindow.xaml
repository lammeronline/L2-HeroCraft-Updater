<Window x:Class="ConfigBuilder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Config Builder" Height="680" Width="1040" MinHeight="620" MinWidth="900"
        Background="#0F1117" Foreground="#F6F1E8" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#191B24" />
        <SolidColorBrush x:Key="SurfaceDeepBrush" Color="#11141C" />
        <SolidColorBrush x:Key="StrokeBrush" Color="#343847" />
        <SolidColorBrush x:Key="StrokeSoftBrush" Color="#292D39" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#B5AD9F" />
        <SolidColorBrush x:Key="TextSoftBrush" Color="#D8D0C3" />
        <SolidColorBrush x:Key="AccentBrush" Color="#C89B3C" />

        <LinearGradientBrush x:Key="WindowBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#141722" Offset="0" />
            <GradientStop Color="#181820" Offset="0.46" />
            <GradientStop Color="#10131A" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource StrokeSoftBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#F6F1E8" />
        </Style>

        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Foreground" Value="#F6F1E8" />
            <Setter Property="Background" Value="#2A2E3A" />
            <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.86" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="9,5" />
            <Setter Property="Foreground" Value="#F6F1E8" />
            <Setter Property="Background" Value="{StaticResource SurfaceDeepBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}" />
            <Setter Property="CaretBrush" Value="#F6F1E8" />
            <Setter Property="SelectionBrush" Value="#735C2B" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextSoftBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Config Builder" FontSize="26" FontWeight="Bold" />
                    <TextBlock Text="Launcher behavior, defaults and publish metadata" Foreground="{StaticResource TextMutedBrush}" FontSize="13" Margin="0,4,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="LOAD" Width="104" Height="40" Margin="0,0,10,0" Click="LoadButton_Click" />
                    <Button Content="SAVE" Width="132" Height="40" Background="{StaticResource AccentBrush}"
                            BorderBrush="{StaticResource AccentBrush}" Foreground="#17120A" FontSize="16"
                            Click="SaveButton_Click" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.25*" />
                    <ColumnDefinition Width="0.95*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource PanelBorderStyle}" Margin="0,0,16,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="Launcher config" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,10" />

                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,10,10">
                            <TextBlock Text="Config file" Style="{StaticResource LabelTextStyle}" />
                            <TextBox x:Name="ConfigFileBox" />
                        </StackPanel>
                        <Button Grid.Row="1" Grid.Column="2" Content="Browse" Width="88" VerticalAlignment="Bottom" Margin="0,0,0,10" Click="BrowseConfigButton_Click" />

                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10">
                            <TextBlock Text="Manifest URL" Style="{StaticResource LabelTextStyle}" />
                            <TextBox x:Name="ManifestUrlBox" />
                        </StackPanel>

                        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10">
                            <TextBlock Text="News page URL" Style="{StaticResource LabelTextStyle}" />
                            <TextBox x:Name="NewsUrlBox" />
                        </StackPanel>

                        <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,12,10">
                            <TextBlock Text="Default mode" Style="{StaticResource LabelTextStyle}" />
                            <TextBox x:Name="DefaultDisplayModeBox" />
                        </StackPanel>

                        <StackPanel Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,0,0,10">
                            <TextBlock Text="Default resolution" Style="{StaticResource LabelTextStyle}" />
                            <TextBox x:Name="DefaultResolutionBox" />
                        </StackPanel>
                        <StackPanel Grid.Row="5" Grid.ColumnSpan="3" Margin="0,0,0,12">
                            <TextBlock Text="Options" Style="{StaticResource LabelTextStyle}" />
                            <WrapPanel>
                                <CheckBox x:Name="ShowClientSettingsBox" Content="Client settings" Margin="0,0,18,6" />
                                <CheckBox x:Name="ShowNewsBox" Content="News panel" Margin="0,0,18,6" />
                                <CheckBox x:Name="RequireUpdateBeforePlayBox" Content="Check before play" Margin="0,0,18,6" />
                                <CheckBox x:Name="AutoLoginEnabledBox" Content="AutoLogin" Margin="0,0,18,6" />
                                <CheckBox x:Name="ShowServerStatusBox" Content="Server status" Margin="0,0,18,6" />
                                <CheckBox x:Name="DefaultAudioMuteBox" Content="Mute by default" Margin="0,0,18,6" />
                            </WrapPanel>
                        </StackPanel>

                        <Grid Grid.Row="6" Grid.ColumnSpan="3" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.25*" />
                                <ColumnDefinition Width="0.7*" />
                                <ColumnDefinition Width="0.7*" />
                                <ColumnDefinition Width="0.8*" />
                                <ColumnDefinition Width="0.9*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Server host / IP" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="ServerStatusHostBox" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                <TextBlock Text="Auth port" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="AuthServerPortBox" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                <TextBlock Text="Game port" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="GameServerPortBox" />
                            </StackPanel>

                            <StackPanel Grid.Column="3" Margin="0,0,10,0">
                                <TextBlock Text="Refresh sec" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="ServerStatusRefreshBox" />
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Timeout ms" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="ServerStatusTimeoutBox" />
                            </StackPanel>
                        </Grid>

                        <Grid Grid.Row="7" Grid.ColumnSpan="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,12,0">
                                <TextBlock Text="Game exe list" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="GameExecutablesBox" Height="74"
                                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                                         TextWrapping="NoWrap" />
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Resolutions" Style="{StaticResource LabelTextStyle}" />
                                <TextBox x:Name="ResolutionsBox" Height="74"
                                         AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12"
                                         TextWrapping="NoWrap" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource PanelBorderStyle}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="Activity log" Style="{StaticResource SectionTitleStyle}" Margin="18,16,18,12" />
                        <TextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12"
                                 Background="#10131A" BorderThickness="0" Padding="14" />
                    </Grid>
                </Border>
            </Grid>

            <TextBlock Grid.Row="2" Text="Upload config.json to /updater/config.json" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" />
        </Grid>
    </Grid>
</Window>

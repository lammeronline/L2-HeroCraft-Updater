<Window x:Class="Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="L2 Modern Updater" Height="720" Width="1120" MinHeight="640" MinWidth="980"
        Background="#111318" Foreground="#F4F6F8" FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#1B2028" />
        <SolidColorBrush x:Key="PanelAltBrush" Color="#232A34" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="#A7B0BD" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2FD17C" />

        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="Padding" Value="18,8" />
            <Setter Property="Foreground" Value="#F7FAFC" />
            <Setter Property="Background" Value="#313B49" />
            <Setter Property="BorderBrush" Value="#465364" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Foreground" Value="#F4F6F8" />
            <Setter Property="Background" Value="#12161D" />
            <Setter Property="BorderBrush" Value="#3E4958" />
            <Setter Property="CaretBrush" Value="#F4F6F8" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Background" Value="#E8EDF3" />
            <Setter Property="BorderBrush" Value="#3E4958" />
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="Background" Value="#F7FAFC" />
            <Setter Property="Padding" Value="8,6" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D7DCE3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
            <Setter Property="Background" Value="#11161D" />
            <Setter Property="BorderBrush" Value="#303947" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="L2 Modern Updater" FontSize="32" FontWeight="Bold" />
                <TextBlock Text="Manifest driven launcher MVP" Foreground="{StaticResource TextMutedBrush}" FontSize="14" Margin="0,4,0,0" />
            </StackPanel>

            <Button x:Name="PlayButton" Grid.Column="1" Content="PLAY" Width="180" Height="48"
                    Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}"
                    Foreground="#07110B" FontSize="18" Click="PlayButton_Click" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1.15*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,18,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="Update source" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />

                        <TextBlock Grid.Row="1" Text="Client folder" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                        <TextBox x:Name="GameDirectoryBox" Grid.Row="1" Grid.Column="1" Margin="0,0,10,10" />
                        <Button Grid.Row="1" Grid.Column="2" Content="Browse" Width="96" Margin="0,0,0,10" Click="BrowseButton_Click" />

                        <TextBlock Grid.Row="2" Text="Manifest" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                        <TextBox x:Name="ManifestSourceBox" Grid.Row="2" Grid.Column="1" Margin="0,0,10,10" />
                        <Button Grid.Row="2" Grid.Column="2" Content="Browse" Width="96" Margin="0,0,0,10" Click="BrowseManifestButton_Click" />

                        <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="CheckButton" Content="Check" Width="112" Margin="0,0,10,0" Click="CheckButton_Click" />
                            <Button x:Name="RepairButton" Content="Repair" Width="112" Margin="0,0,10,0" Background="#6A4CC2" BorderBrush="#7B5ED2" Click="RepairButton_Click" />
                            <Button x:Name="UpdateButton" Content="Update" Width="112" Background="#3B6FD8" BorderBrush="#4D82EA" Click="UpdateButton_Click" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8" Margin="0,18,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.ColumnSpan="4" Text="Client settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />

                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                            <TextBlock Text="Display mode" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,6" />
                            <ComboBox x:Name="DisplayModeBox" SelectionChanged="ClientSettings_Changed">
                                <ComboBoxItem Content="Windowed" IsSelected="True" />
                                <ComboBoxItem Content="Borderless" />
                                <ComboBoxItem Content="Fullscreen" />
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,12,0">
                            <TextBlock Text="Resolution" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,6" />
                            <ComboBox x:Name="ResolutionBox" SelectionChanged="ClientSettings_Changed">
                                <ComboBoxItem Content="1024x768" />
                                <ComboBoxItem Content="1280x720" />
                                <ComboBoxItem Content="1366x768" />
                                <ComboBoxItem Content="1600x900" />
                                <ComboBoxItem Content="1920x1080" IsSelected="True" />
                                <ComboBoxItem Content="2560x1440" />
                            </ComboBox>
                        </StackPanel>

                        <CheckBox x:Name="AudioMuteBox" Grid.Row="1" Grid.Column="2" Content="Mute audio" Margin="0,24,14,0" Checked="ClientSettings_Changed" Unchecked="ClientSettings_Changed" />
                        <Button Grid.Row="1" Grid.Column="3" Content="Apply" Width="96" Margin="0,16,0,0" Click="ApplySettingsButton_Click" />
                    </Grid>
                </Border>

                <Border Grid.Row="2" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8" Margin="0,18,0,18">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="StatusText" Text="Idle" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock x:Name="TransferText" Grid.Column="1" Text="0 / 0" Foreground="{StaticResource TextMutedBrush}" />
                        </Grid>
                        <ProgressBar x:Name="MainProgressBar" Minimum="0" Maximum="100" Value="0" />
                        <TextBlock x:Name="CurrentFileText" Text="Waiting for manifest" Foreground="{StaticResource TextMutedBrush}" Margin="0,10,0,0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="3" Background="{StaticResource PanelBrush}" BorderBrush="#2B333F" BorderThickness="1" Padding="18" CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Text="Log" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />
                        <TextBox x:Name="LogBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" />
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Column="1" Background="{StaticResource PanelAltBrush}" BorderBrush="#303947" BorderThickness="1" Padding="18" CornerRadius="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="News" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14" />
                    <ListBox x:Name="NewsList" Grid.Row="1" Background="#171C24" BorderBrush="#303947" Foreground="#F4F6F8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="8">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding PublishedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,2,0,6" />
                                    <TextBlock Text="{Binding Body}" Foreground="#D7DCE3" TextWrapping="Wrap" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="Stage 1 MVP: fast check, repair, manifest, update, launch" Foreground="{StaticResource TextMutedBrush}" Margin="0,18,0,0" />
    </Grid>
</Window>
